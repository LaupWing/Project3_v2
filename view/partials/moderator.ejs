<div class="container moderator">
    <ul class="profiles">
        <% data.forEach(person=>{ %>
            <li  class="<%= person.id %>"><%=person.name%></li>
        <% }) %>
    </ul>
<!-- <% data.forEach(person=>{ %>
    <div class="chat" id="<%= person.id %>">
        <div class="user">
            <h2><%= person.name %></h2>
            <p><%= person.location %></p>
        </div>
        <div class="messages">
    
        </div>
        <form action="" class="sendMsg">
            <input type="text">
            <button type="submit">Send</button>
        </form>
    </div>
<% }) %> -->
    <div class="chat-container">
        <!-- <div class="chat" id="">
            <div class="user">
                <h2>Naam</h2>
                <p>Woonplaats</p>
            </div>
            <div class="messages">
        
            </div>
            <form action="" class="sendMsg">
                <input type="text">
                <button type="submit">Send</button>
            </form>
        </div> -->
        <div class="no-visitors">
            '<h2>Nog geen bezoekers</h2>
        </div>
    </div>
    <form class="user-info" action="">
    
    </form>
</div>

<script src="scripts/moderator.js"></script>